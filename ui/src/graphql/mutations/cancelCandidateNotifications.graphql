mutation cancelCandidateNotifications($candidateId: ID!) {
  cancelCandidateNotifications(id: $candidateId) {
    result {
      id
    }
    errors {
      code
      fields
      message
      shortMessage
      vars
    }
  }
}
