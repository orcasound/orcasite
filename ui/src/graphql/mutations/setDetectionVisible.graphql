mutation setDetectionVisible($id: ID!, $visible: Boolean!) {
  setDetectionVisible(id: $id, input: { visible: $visible }) {
    result {
      id
      visible
    }
    errors {
      code
      fields
      message
      shortMessage
      vars
    }
  }
}
