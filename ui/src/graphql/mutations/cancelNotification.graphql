mutation cancelNotification($id: ID!) {
  cancelNotification(id: $id) {
    result {
      id
      meta
      active
      insertedAt
      targetCount
      notifiedCount
      progress
      finished
    }
    errors {
      code
      fields
      message
      shortMessage
      vars
    }
  }
}
