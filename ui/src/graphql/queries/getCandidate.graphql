query candidate($id: ID!) {
  candidate(id: $id) {
    id
    minTime
    maxTime
    detectionCount
    visible
    category
    feed {
      id
      slug
      name
      nodeName
      bucket
      cloudfrontUrl
    }
    detections {
      id
      category
      description
      listenerCount
      playlistTimestamp
      playerOffset
      timestamp
      visible
      sourceIp
    }
  }
}
