query notificationsForCandidate(
  $candidateId: String!
  $eventType: NotificationEventType
) {
  notificationsForCandidate(candidateId: $candidateId, eventType: $eventType) {
    id
    eventType
    active
    insertedAt
    targetCount
    notifiedCount
    notifiedCountUpdatedAt
    progress
    finished
  }
}
